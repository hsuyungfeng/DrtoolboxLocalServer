# DrtoolboxLocalServer - Handoff State (2026-05-25)

## 🎯 Current Status
- **Active Phase**: Phase 3 Extensions (Stability, Automation & Hybrid RAG)
- **Model**: `llama-qwen` (Qwen-35B-A3B via ROCm Docker on port 8080)
- **Status**: System is fully live, synchronized, and optimized for deep medical reasoning and automated data collection.

## ✅ Completed in Last Session
1. **Model Migration & LLM Optimization**:
   - Switched all services (Hermes, RAG, API) to `llama-qwen`.
   - Fixed NameErrors and import paths for direct script execution (`uv run`).
   - Implemented **Streaming (SSE)** for real-time chat in the Web UI.

2. **Hybrid RAG & Deep Reasoning (PageIndex)**:
   - Built a hybrid engine combining fast N-gram keyword matching with semantic reasoning trees.
   - Launched a background worker (`ThreadPoolExecutor`, 2 workers) to generate professional medical summaries for 1,500+ documents.
   - Added thread locking and memory caching for the RAG index to ensure safety and speed.

3. **Human-in-the-Loop Feedback Loop**:
   - **Nightly Fact-Check**: Uses Web Search (DuckDuckGo) to verify daily chat logs and generate correction drafts with evidence links.
   - **Proactive QA Generator**: Automatically identifies clinic services and simulates patient QAs based on local documents.
   - **Curation Dashboard**: Upgraded with editable prompts, "Discard" buttons, and an "Auto-Hide after approval" workflow.

4. **Automation & Sync**:
   - **Auto-Post System**: Implemented a Playwright-based web automation script (`auto_post_to_toolbox.py`) that successfully synced 41 curated articles to the official `doctor-toolbox.com` backend.
   - **Weekly CRM Insights**: Created an automated engine to analyze patient trends and pain points every Monday at 4:00 AM.

5. **Stability & Security**:
   - Hardened file uploads with `secure_filename` and Private Network Access headers.
   - Implemented a **Safe Archive** strategy for processed files.
   - Installed system dependencies for OCR (`tesseract`), legacy doc conversion (`libreoffice`), and audio transcription (`faster-whisper`).

## 🛠️ Pending / Remaining Work
- **PageIndex Ingestion**: Monitor the background indexing thread as it finishes processing the remaining documents.
- **Golden Set Evaluation**: Expand `data/evaluation/golden_set.json` with more complex medical scenarios and run `scripts/evaluate_rag.py`.
- **UI Refinements**: Consider adding a specific view for the generated Weekly CRM Insights reports.
- **Long Context Management**: Implement Direction C (Sliding Window Summary) if VRAM pressure increases during long sessions.

## 📁 Key Files & Directories
- `src/agent/hermes_core.py`: Unified "Brain" of the system.
- `src/rag_engine.py`: Hybrid N-gram + PageIndex search logic.
- `scripts/auto_post_to_toolbox.py`: Web automation sync script.
- `data/verified_training_data.jsonl`: Curated high-quality dataset (Committed).
- `data/pageindex/`: Pre-computed reasoning trees for documents.

## 🚀 Resume Instructions
1. Ensure the `llama-qwen` Docker container is running on port 8080.
2. Start the backend: `uv run python src/api/app.py`.
3. Test the high-risk alert system by asking about "劇烈疼痛" in the chat tab.
4. Review the latest **PROACTIVE** items in the Curation tab to continue building the training set.

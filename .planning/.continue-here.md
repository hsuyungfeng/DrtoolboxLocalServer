---
context: phase_5_planning
phase: 5-Enterprise-Dashboard-CloudSync
task: 0
total_tasks: 5
status: in_progress
last_updated: 2026-05-12T04:09:57+08:00
---

# Phase 5 — Dashboard Design & Doctor-Toolbox Integration (Planning Paused)

## ⚠️ Blocker: Doctor-Toolbox API Spec Needed

Cloud sync (SYNC-01, SYNC-02) is **blocked** until user provides the doctor-toolbox.com API documentation.

**Action required from user before implementing sync:**
- API authentication method (JWT? API key? OAuth?)
- Endpoints for patient data sync
- Payload format and field mapping
- Rate limits and conflict resolution approach

**Dashboard has NO external dependency — start there.**

<current_state>
Phase 5 planning was in progress (plan mode entered). An Explore agent completed
full codebase mapping. Planning paused when /gsd-pause-work was invoked before
the plan document was written. The Explore results are preserved in context_notes
of HANDOFF.json. Resume by writing the Phase 5 plan, starting with dashboard.
</current_state>

<completed_work>
- Phase 4 committed (8db4cfb): hermes_core, pattern_learner, intent_router, hermes_cli, verify_phase4
- GPU optimization attempted → llama-server binary missing from /tmp (ephemeral storage)
- GPU optimization DEFERRED (binary not persistent; use build_llama_cuda.sh to rebuild)
- Explore agent completed: full codebase map (routes, schema, templates, cloud_backend)
- scripts/gpu_layer_optimization.sh created (for future use when binary rebuilt)
</completed_work>

<remaining_work>
Phase 5 tasks (in priority order):

1. **[BLOCKED on API spec]** Get doctor-toolbox.com API documentation from user
2. **[READY]** Write Phase 5 plan document (dashboard architecture + interaction functions)
3. **[READY]** Build base Jinja2 layout template (shared nav, Bootstrap 5, zh-TW)
4. **[READY]** Build analytics dashboard:
   - Clinic metrics (clinic_info, clinic_daily_log)
   - Patient communication stats (patient_conversations unread/escalated)
   - Hermes agent usage (auto_skills, skill_metrics)
   - LINE bot activity
5. **[READY]** Build interaction functions (staff actions, not just read-only)
6. **[BLOCKED]** Cloud sync stubs (SYNC-01, SYNC-02) — needs API spec
</remaining_work>

<decisions_made>
- Dashboard first, cloud sync second — API spec unavailable, dashboard is self-contained
- GPU optimization deferred — /tmp is ephemeral, binary disappears across sessions
- Phase 5 scope expanded: user wants dashboard + interaction functions (beyond ROADMAP SYNC-01/02)
- Use existing Bootstrap 5 + Jinja2 + Traditional Chinese stack (no framework change)
- analytics dashboard will query: clinic_daily_log, patient_conversations, auto_skills, skill_metrics
</decisions_made>

<blockers>
- doctor-toolbox.com API spec missing → cloud sync cannot be implemented yet
- llama-server binary ephemeral → GPU optimization needs permanent install path
</blockers>

## Codebase Quick Reference (from Explore agent)

### Existing Templates
| File | Route | Purpose |
|------|-------|---------|
| `patient_dashboard.html` | `/dashboard/patient/<id>` | Read-only patient view |
| `staff_dashboard.html` | `/dashboard/staff/patients` | Searchable patient list |
| `staff_patient_detail.html` | `/dashboard/staff/patient/<id>` | Patient edit form |
| `staff_inbox.html` | (staff_inbox_bp) | Polling message inbox |
| `staff_conversation.html` | `/dashboard/staff/conversation/<id>` | Conversation thread |

### Key Database Tables for Analytics
- `clinic_daily_log` — daily visit counts, revenue, etc.
- `clinic_info` — staff_count, num_beds, specialties
- `patient_conversations` — unread_flag, escalated_flag, rag_confidence
- `auto_skills` — skill name, usage count
- `skill_metrics` — usage tracking for Hermes auto-skills
- `appointments` — appointment_date, status

### Existing Routes (avoid conflicts)
- `/dashboard/patient/<id>` — patient view
- `/dashboard/staff/patients` — patient list
- `/dashboard/staff/patient/<id>` — patient detail
- `/dashboard/staff/conversation/<id>` — conversation thread
- **New Phase 5 routes should use `/dashboard/analytics/` prefix**

### Auth Pattern
All staff routes use `require_staff_id` decorator (checks `X-Staff-ID` header).
This is a stub — Phase 5 keeps same pattern; real JWT deferred to future.

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full context_notes with Explore findings
2. `src/api/app.py` — blueprint registration, understand all existing routes
3. `schema/clinic.db.sql` — table schemas for analytics queries
4. `src/api/routes/staff_dashboard.py` — existing dashboard pattern to extend
5. `src/templates/staff_dashboard.html` — Bootstrap 5 pattern to follow

## Critical Anti-Patterns (do NOT repeat these)
- **[ANTI-PATTERN] --gpu-layers 50 on Qwen 3.6-35B** → CUDA VRAM crash. Always use --gpu-layers 20. (Phase 4 discovery)
- **[ANTI-PATTERN] Assuming llama-server persists in /tmp** → Binary disappears across sessions. Re-run build_llama_cuda.sh each time, or install to permanent path.
- **[ANTI-PATTERN] Standalone templates without shared nav** → 5 existing templates each have duplicated CSS. Phase 5 MUST introduce a base template to avoid growing the debt.

## Infrastructure State
- **llama-server**: NOT running (binary gone from /tmp after session end)
- **DeepSeek API**: Configured in .env (DEEPSEEK_API_KEY), model deepseek-chat
- **clinic.db**: data/local_db/clinic.db (15 tables)
- **Python venv**: .venv/ (Python 3.13.11)
- **GPU**: RTX 2080 Ti, 22GB VRAM, Driver 580.159.03
- **Flask API**: not currently running

## Pre-Execution Critique Required
- Design artifact: Phase 5 plan NOT YET WRITTEN (planning paused)
- Next step: Resume plan mode, write Phase 5 PLAN.md
- Gate: Write plan → user approves → execute

<context>
User wants Phase 5 to focus on:
1. Analytics dashboard for clinic operations
2. Interaction functions (staff actions, not just read-only views)  
3. Cloud sync with doctor-toolbox.com (blocked on API spec — user will provide)
4. GPU optimization (deferred — binary needs permanent home)

The Explore agent mapped the entire codebase. Bootstrap 5 + Jinja2 + Traditional
Chinese is the established stack. 5 templates exist but each is standalone with
no shared nav or base layout. Phase 5 needs a base template as the first step.
Analytics data is available in clinic_daily_log, patient_conversations, auto_skills.
</context>

<next_action>
1. Ask user for doctor-toolbox.com API spec (or confirm it's deferred)
2. Resume /gsd-plan-phase 5 to write the Phase 5 PLAN.md
3. First implementation task: create src/templates/base.html (shared nav + Bootstrap 5)
4. Second: add /dashboard/analytics/ routes with clinic metrics
</next_action>

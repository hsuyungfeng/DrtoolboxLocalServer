---
context: research
phase: 4-Hermes-Agent
task: 5
total_tasks: 5
status: in_progress
last_updated: 2026-05-11T15:48:57+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: INFERENCE ENGINE SEGFAULT — Running any large model (Gemma4 OR Qwen3.6) using current `llama-server` builds (Vulkan via brew OR manual CUDA build) crashes at `load_tensors` with `程式記憶體區段錯誤 (核心已傾印)`. This indicates corrupted driver state or binary mismatch.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Swapping models for Fix | Attempting different model files won't fix systemic binary crashes | blocking | Debug build/env instead of downloading more models |

<current_state>
Phase 4 Python logic is complete. We manually verified and completed the `Qwen3.6-35B-A3B.Q3_K_M.gguf` download into `models/` (rescued from cache). 
However, trying to launch `llama-server` with EITHER model leads to an IMMEDIATE `程式記憶體區段錯誤 (核心已傾印) (Segmentation Fault)` right as tensors finish printing metadata.
</current_state>

<completed_work>
Completed Tasks:
- Model Download 1: `gemma-4-31b-jang-crack-Q3_K_M.gguf` - Done
- Model Download 2: `Qwen3.6-35B-A3B.Q3_K_M.gguf` - Done and Rescued to `models/`
- Infrastructure 1: Updated llama.cpp to version 9100 via Homebrew - Done
- Infrastructure 2: Completed local build of llama.cpp with CUDA - Done
- Integration: Verified that `gemma4` architecture tags are fully recognized by current binary - Done
</completed_work>

<remaining_work>
- Task 5: Resolve system Segfault and establish running HTTP backend
- Task 6: Final chat routing verification using `uv run python scripts/hermes_cli.py`
</remaining_work>

<decisions_made>
- Abandoned further testing on Gemma-4 for today, switched to Qwen 3.6 fallback, but confirmed systemic error.
</decisions_made>

<blockers>
- [LLAMA_SEGFAULT]: The binary crashes right after detecting `Vulkan0` device when destruction of internal fences is triggered or memory mapping completes. Likely GPU device context issue.
</blockers>

## Required Reading (in order)
1. Previous console output — See the dual crash traces from Gemma and Qwen.

## Critical Anti-Patterns (do NOT repeat these)
- [BAD_ASSUMPTION]: Assuming different flags like `--no-mmap` will fix a driver level crash → [structural mitigation] Reboot system or clean environment variables.

## Infrastructure State
- `models/`: Contains both GGUFs ready to load.
- `llama.cpp`: Both `/usr/local/bin/llama-server` (Homebrew) and `./llama.cpp/build/bin/llama-server` (CUDA) are present. Both crash.

<context>
Total logic and architecture are finalized, but underlying GPU environment is unstable right now.
</context>

<next_action>
Start with: Try rebooting user computer OR fully purging / rebuilding the inference server with `GGML_CUDA=1` targeting strictly Compute Capability 7.5 for the 2080Ti without mixing with Homebrew.
</next_action>

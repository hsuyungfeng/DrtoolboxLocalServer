---
context: phase
phase: 4
task: 4
total_tasks: 4
status: blocked
last_updated: 2026-05-11T12:51:22+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Background Download — The `gemma-4-31b-jang-crack-Q3_K_M.gguf` model is currently downloading in the background to `.cache/` and hasn't been finalized to `models/` yet. Do not attempt to start `llama-server` until the download process is fully completed.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| LLM Out of Memory (OOM) | Loading a 31B model with 4-bit quantization on a 22GB 2080Ti along with desktop apps crashes Ollama. | advisory | We are downloading the smaller Q3_K_M GGUF format and switching to `llama.cpp` to squeeze into the available VRAM. |

<current_state>
We have fully implemented all Phase 4 Python scripts (`hermes_core.py`, `pattern_learner.py`, `skill_manager.py`, `skill_generator.py`, `hermes_cli.py`) and updated the SQLite schema. We are currently blocked waiting for the background huggingface-cli download to complete.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Implemented `hermes_core.py` and `pattern_learner.py` - Done
- Task 2: Updated SQLite schema and implemented `skill_manager.py` / `skill_generator.py` - Done
- Task 3: Created and tested `hermes_cli.py` (fixed SQLite paths and RAG TypeError) - Done
- Task 4: Reverted API to target `http://127.0.0.1:8081/v1/chat/completions` for `llama.cpp` compatibility - Done
</completed_work>

<remaining_work>
- Phase 4 is technically complete code-wise.
- Need to verify the system end-to-end once `llama-server` is up.
</remaining_work>

<decisions_made>
- Decided to use `llama.cpp` directly with a Q3_K_M GGUF instead of Ollama's `gemma4:31b` to avoid VRAM overflow on the 2080Ti.
- Reverted the LLM API endpoints in the codebase back to port `8081` and removed the `"model"` enforcement constraint in the POST payload so `llama.cpp` can handle it natively.
</decisions_made>

<blockers>
- Model Download: The background process downloading `gemma-4-31b-jang-crack-Q3_K_M.gguf` must finish before `llama-server` can be started.
</blockers>

## Required Reading (in order)
1. `src/agent/hermes_core.py` — The core agent logic interfacing with `llama.cpp`
2. `scripts/hermes_cli.py` — The command-line tool for the user to interact with the system.

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Running `llama-server` before the download completes → It will throw a `No such file or directory` error. Wait for the download to finish.

## Infrastructure State
- Download: `huggingface-cli download` is running in the background.
- LLM Server: `llama-server` is currently offline (waiting for model).

<context>
The user tried to run `llama-server` manually in the terminal but it failed because the download process hasn't finished moving the file from `.cache/` to the `models/` folder. The system is paused to let the download finish.
</context>

<next_action>
Start with: Verify if the `models/gemma-4-31b-jang-crack-Q3_K_M.gguf` file exists and is fully downloaded (approx 15.3GB), then ask the user to start `llama-server`.
</next_action>

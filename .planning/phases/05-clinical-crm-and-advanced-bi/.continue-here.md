---
context: phase
phase: 05-clinical-crm-and-advanced-bi
task: 2
total_tasks: 7
status: in_progress
last_updated: 2026-07-21T10:07:39.637Z
---

<current_state>
We have resolved the severe OOM issues with the LLM. The 128K context + 9B model was too large for the 12GB RTX 3060, so we reduced the context to 32768 in the `llama-server.service` and `llama_config.json`. The backend health monitor crashed when the LLM went offline because it was hardcoded to use `docker restart`, so we updated `src/services/health_monitor_service.py` to use `systemctl`. The LLM is now humming along at 30+ t/s and the backend is running. We are ready to proceed with Real-time Staff Alerting.
</current_state>

<completed_work>

Completed Tasks:
- Task 1.1: Build Patient Detail View - Done
- Task 1.2: ehrapy Dashboard Integration - Done
- Task 3.1: OTC Drug Localization - Done (Integrated mapping and RAG injection)
</completed_work>

<remaining_work>

- Task 2.1: Implement High-risk Symptom Notification
- Task 2.2: Add Risk Alerting Background Thread
- Task 3.2: Reservation CTA Integration
- Task 3.3: Patient Engagement Follow-up Questions
- Task 4.1: Populate clinic.db with Diverse Simulated Patients
- Task 4.2: E2E Verification Tests
</remaining_work>

<decisions_made>

- Decided to reduce context size to 32K because 128K + multimodal projection + 9B model exceeds 12GB VRAM.
- Fixed the health_monitor_service to use `systemctl --user restart llama-server` instead of docker commands.
</decisions_made>

<blockers>
</blockers>

## Required Reading (in order)
<!-- List documents the resuming agent must read before acting -->
1. `.planning/phases/05-clinical-crm-and-advanced-bi/05-PLAN.md` — To see the full task list and definitions.

## Infrastructure State
<!-- Running services, external state, environment specifics -->
- `llama-server`: Running as a systemd service (`systemctl --user status llama-server`) on port 8080.
- `backend`: Running natively via `uv run python3 src/api/app.py` on port 5000.

<context>
The environment is now stable after fixing crashes. Next step is to implement the real-time staff alerting (Task 2.1).
</context>

<next_action>
Start with: Proceed with Task 2.1 (Implement High-risk Symptom Notification)
</next_action>

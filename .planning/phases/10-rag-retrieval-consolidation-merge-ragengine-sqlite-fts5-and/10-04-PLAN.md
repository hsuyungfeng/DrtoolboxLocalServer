---
phase: 10-rag-retrieval-consolidation-merge-ragengine-sqlite-fts5-and
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - src/rag/normalize.py
  - src/rag/ingest.py
  - src/rag_engine.py
  - src/rag/hybrid.py
  - tests/test_normalize.py
autonomous: true
requirements: []
tags: [rag, opencc, observability]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Text ingested from data/documents/general/ (confirmed Simplified Chinese source) is converted to Traditional Chinese before being chunked/stored, in both the unified RAGEngine write path and the standalone DocumentIngestor.ingest() path used by /api/v1/rag/ingest."
    - "A normalization failure (e.g. unexpected input) degrades to storing the raw un-normalized text with a logged warning — it never blocks ingestion."
    - "Every answer's rag_context is traceable in logs to the specific chunk_ids and fused RRF scores that produced it."
  artifacts:
    - src/rag/normalize.py (normalize_to_traditional())
    - tests/test_normalize.py
  key_links:
    - "normalize_to_traditional() must be called before chunk_text() in both ingestion paths (unified RAGEngine path and DocumentIngestor.parse_document()), not after — normalizing already-chunked text would need to run twice."
    - "HybridRetriever.get_scored_chunks() must expose chunk_id alongside fused content so _get_context() has something concrete to log per D-09 — plain content strings alone aren't loggable identifiers."
</must_haves>
---

<objective>
Close the two remaining book-to-skil.md gaps CONTEXT.md scoped into this phase: convert Simplified Chinese source text to Traditional Chinese before it can leak into a patient-facing answer (D-08), and log which chunk_ids and fused RRF scores actually produced each answer's context (D-09). This is Stage 4, the final stage of the phase.

Purpose: `data/documents/general/`'s 848 files are confirmed Simplified Chinese (sampled and read directly in RESEARCH.md), but `RAGEngine.query_integrated()`'s system prompt hard-requires `嚴禁簡體中文` (Simplified Chinese strictly forbidden) in every answer — raw Simplified source text retrieved verbatim risks violating that requirement. Separately, once a wrong answer is reported, there's currently no way to tell "bad retrieval" from "bad LLM reasoning" without chunk-level detail in the logs.
Output: `src/rag/normalize.py` (new), normalization wired into both ingestion paths, and `_get_context()`'s existing `logger.info` calls extended with chunk-id + fused-score detail.
</objective>

<assumption_delta_decision>
**Noun now primary:** the answer-context retriever called from `RAGEngine._get_context()` (unchanged from Plans 10-02/10-03 — `HybridRetriever` remains the sole call site).
**Decision:** `promote` (already executed in Plan 10-02, reinforced by Plan 10-03's chunk_id parity). This plan's only touch on `HybridRetriever` is adding chunk_id to its return value for logging purposes — it does not change which component is called or how fusion works.
**Rationale:** Keeping the promotion decision stable across all four plans avoids re-litigating the "one call site, sparse-only only as internal fallback" architecture for what is, in this plan, purely an observability addition.
</assumption_delta_decision>

<execution_context>
@/home/hsuyungfeng/.claude/gsd-core/workflows/execute-plan.md
@/home/hsuyungfeng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-rag-retrieval-consolidation-merge-ragengine-sqlite-fts5-and/10-CONTEXT.md
@.planning/phases/10-rag-retrieval-consolidation-merge-ragengine-sqlite-fts5-and/10-RESEARCH.md
@.planning/phases/10-rag-retrieval-consolidation-merge-ragengine-sqlite-fts5-and/10-PATTERNS.md
@.planning/phases/10-rag-retrieval-consolidation-merge-ragengine-sqlite-fts5-and/10-03-SUMMARY.md
@src/rag/ingest.py
@src/rag_engine.py
@src/rag/hybrid.py
</context>

## Artifacts this phase produces

- `src/rag/normalize.py` — new file: `normalize_to_traditional(text: str) -> str`
- `tests/test_normalize.py` — new test file
- `reciprocal_rank_fusion()`'s new return shape `List[Tuple[str, str, float]]` (key, content, fused_score) — changed in `src/rag/hybrid.py` (was `List[str]` in Plan 10-02)
- `HybridRetriever.get_scored_chunks()`'s matching new return shape — changed in `src/rag/hybrid.py`
- The `"Selected chunks:"` log line — new `logger.info` call inside `RAGEngine._get_context()` in `src/rag_engine.py`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Simplified-to-Traditional normalization pass (D-08)</name>
  <read_first>src/rag/ingest.py (lines 157-201, `parse_document()` — per-format try/except structure to match), src/rag_engine.py (lines 242-260, `ingest_special_data`/`ingest_general_data`/`_ingest_unified` from Plan 10-03 — the unified write path's normalization insertion point), data/documents/general/medical_kb_batch_1.txt (confirmed Simplified source, e.g. `【疾病描述】：肺泡蛋白质沉积症(简称PAP)`)</read_first>
  <files>src/rag/normalize.py, tests/test_normalize.py</files>
  <behavior>
    - `normalize_to_traditional("简称")` returns Traditional Chinese output (`簡稱`), not the Simplified input unchanged.
    - `normalize_to_traditional("")` (empty string) returns `""` without raising.
    - `normalize_to_traditional(text)` on already-Traditional or mixed/garbled OCR text (e.g. `"Mini-plastic Surgery UH - HARE"`) does not raise — OpenCC only maps recognized Han characters and passes everything else through unchanged.
    - If the underlying `OpenCC` converter raises for any reason, `normalize_to_traditional(text)` catches it, logs a warning, and returns the original `text` unchanged (never blocks ingestion).
  </behavior>
  <action>
  Create `src/rag/normalize.py`: `from opencc import OpenCC` at module level, `_converter = OpenCC('s2twp')` (Simplified → Traditional-Taiwan with phrase-level substitution, matching the clinic's Taiwan-Traditional requirement — not `'s2t'`, which is character-only). Define `normalize_to_traditional(text: str) -> str`: if `not text`, return `text` as-is; otherwise `try: return _converter.convert(text) except Exception as e: logger.warning(f"OpenCC normalization failed, using raw text: {e}"); return text`.

  Wire it into BOTH ingestion paths:
  1. In `src/rag/ingest.py`'s `DocumentIngestor.parse_document()`, import `from rag.normalize import normalize_to_traditional` and apply it to the `text` variable immediately before the final `return text` (after all the per-format `if ext == ...` branches, so it applies uniformly regardless of source format) — this covers the standalone `/api/v1/rag/ingest` path.
  2. In `src/rag_engine.py`'s `_ingest_unified()` (added in Plan 10-03), import `from src.rag.normalize import normalize_to_traditional` and apply it to `doc['content']` before computing `content_hash` and before calling `chunk_text()` — normalizing before chunking (not after) means each chunk only needs original-format detection once, and the content hash reflects the normalized text so a Simplified-vs-already-normalized re-ingest doesn't falsely skip.

  Create `tests/test_normalize.py` covering the 4 behavior cases above.
  </action>
  <verify>
    <automated>cd /home/hsuyungfeng/DrtoolboxLocalServer && uv run pytest tests/test_normalize.py -x -q</automated>
  </verify>
  <acceptance_criteria>
  - All 4 behavior cases pass.
  - `grep -n "normalize_to_traditional" src/rag/ingest.py src/rag_engine.py` shows both call sites.
  </acceptance_criteria>
  <done>Both ingestion paths (standalone DocumentIngestor.ingest() and the unified RAGEngine write path) normalize Simplified Chinese source text to Traditional before chunking, with a non-blocking fallback on normalization failure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Chunk-level retrieval observability logging (D-09)</name>
  <read_first>src/rag_engine.py (lines 577-598, `_get_context()`'s SimpleIndex/HybridRetriever block and its existing `logger.info` calls), src/rag/hybrid.py (from Plan 10-02 — `HybridRetriever.get_scored_chunks()` currently returns `List[str]`, content only)</read_first>
  <files>src/rag/hybrid.py, src/rag_engine.py</files>
  <action>
  In `src/rag/hybrid.py`, change `reciprocal_rank_fusion()`'s return type from `List[str]` to `List[Tuple[str, str, float]]` — `(chunk_id_or_key, content, fused_score)` — by returning `[(kk, content_by_key[kk], fused_scores[kk]) for kk in ranked_keys]` instead of just content. Update `HybridRetriever.get_scored_chunks()` to match: on the success path, return the fused triples directly; on the D-06 fallback path (Chroma failure), return `[(_key(content), content, score) for score, content in sparse_ranked]` so the shape stays consistent whether or not fusion happened. `_key()` remains the content-hash defined in Plan 10-02 (Plan 10-03's real `chunk_id`s now make this hash increasingly meaningful post-unification, but the function signature does not need to change to "know" that).

  In `src/rag_engine.py`'s `_get_context()`, update the fused-chunks consumption loop to unpack the new 3-tuple shape: `for key, chunk, fused_score in fused_chunks:` instead of `for chunk in fused_chunks:`. Keep the `seen` dedup set (dedup on `chunk` content, unchanged), the top-4 cap, and both price-redaction `re.sub` lines exactly as they are. Extend the existing `logger.info(f"[_get_context] SimpleIndex done. Context length: {len(rag_context)}")` line (or add one adjacent to it) to also log the `key`/`fused_score` pairs actually selected into `top_chunks` — e.g. `logger.info(f"[_get_context] Selected chunks: {[(key, round(fused_score, 4)) for key, _, fused_score in selected]}")` — this is the D-09 deliverable: which chunks (by their correlation key) and what fused scores produced this answer's context, extending the existing `logger.info` instrumentation rather than building a new logging subsystem.

  Update `tests/test_hybrid_retriever.py` (from Plan 10-02) to assert the new 3-tuple return shape on all 5 existing behavior cases — this is expected to require edits to that test file since the function signature changed, not a new file.
  </action>
  <verify>
    <automated>cd /home/hsuyungfeng/DrtoolboxLocalServer && uv run pytest tests/test_hybrid_retriever.py tests/test_rag_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
  - `uv run pytest tests/test_hybrid_retriever.py tests/test_rag_engine.py -x -q` passes (both the updated hybrid-retriever shape and the unchanged end-to-end `query_integrated()` behavior).
  - `grep -n "Selected chunks" src/rag_engine.py` shows the new D-09 log line inside `_get_context()`.
  </acceptance_criteria>
  <done>`_get_context()` logs the chunk correlation keys and fused RRF scores that produced each answer's rag_context, extending the existing logger.info instrumentation, with no change to the top-4/dedup/price-redaction post-processing shape.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify no Simplified-only characters leak through and confirm observability logs are readable</name>
  <read_first>data/documents/general/medical_kb_batch_1.txt (known-Simplified sample already confirmed in RESEARCH.md)</read_first>
  <files>(no new files — verification run ad hoc, not committed)</files>
  <action>
  Run a small driver script: read `data/documents/general/medical_kb_batch_1.txt`, call `normalize_to_traditional()` on its content, and check the output against a small set of known Simplified-only characters that have distinct Traditional forms relevant to this corpus (`简`→`簡`, `该`→`該`, `发`→`發`, `诊`→`診`) — confirm none of the Simplified forms remain in the normalized output while the Traditional forms are present. Then construct a `RAGEngine()`, call `query_integrated("肉毒桿菌術後多久可以洗臉？")` with `engine.reasoner` swapped for a `MockReasoner` (matching `tests/test_rag_engine.py`'s existing pattern) so no real LLM call is needed, and capture log output (e.g. via Python's `logging` module with a `caplog`-style handler, or redirect to a temp file) to confirm a `"Selected chunks:"` log line appears with at least one `(key, fused_score)` tuple.
  </action>
  <verify>
    <automated>cd /home/hsuyungfeng/DrtoolboxLocalServer && python3 -c "
from src.rag.normalize import normalize_to_traditional
text = open('data/documents/general/medical_kb_batch_1.txt', encoding='utf-8').read()
normalized = normalize_to_traditional(text)
simplified_chars = ['简', '该', '发', '诊']
traditional_chars = ['簡', '該', '發', '診']
leaked = [c for c in simplified_chars if c in normalized]
assert not leaked, f'Simplified characters leaked through normalization: {leaked}'
present = [c for c in traditional_chars if c in normalized]
assert present, 'no expected Traditional characters found — normalization may not have run'
print(f'normalization OK, no leaked Simplified chars, found Traditional: {present}')
"</automated>
  </verify>
  <acceptance_criteria>
  - The verification script prints `normalization OK, no leaked Simplified chars, found Traditional: [...]` with a non-empty Traditional character list.
  - Manual/log-capture confirmation that `_get_context()` emits a `"Selected chunks:"` log line containing chunk keys and fused scores for a real query.
  </acceptance_criteria>
  <done>A real Simplified-source document normalizes cleanly with no target Simplified characters surviving, and retrieval observability logs are confirmed to surface chunk-level detail for a real query — closing both D-08 and D-09, the last two locked decisions in this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retrieved chunk text → LLM prompt → patient-facing answer | Normalization is a best-effort language-compliance pass on this boundary, not a hard content filter. |
| `_get_context()` → application logs | New chunk-level detail (correlation keys, fused scores) is written to the existing log destination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10 | Tampering | `normalize_to_traditional()` failure handling | medium | mitigate | Per the established per-format try/except convention in `parse_document()`, an OpenCC failure logs a warning and falls back to raw (possibly Simplified) text rather than blocking ingestion — this is a best-effort compliance measure for the `嚴禁簡體中文` system-prompt requirement, not a hard guarantee. RESEARCH.md Pitfall 2 already documents that some source content (mixed/garbled OCR in `data/documents/special/`) will remain imperfect regardless of this pass — that is an acknowledged pre-existing data-quality limitation, explicitly out of scope to fully solve in this phase. |
| T-10-11 | Information Disclosure | D-09's new chunk-id + fused-score log lines | low | accept | Logs already record chunk counts/context lengths throughout `_get_context()` today (existing `logger.info` convention); the new `"Selected chunks:"` line adds correlation keys and scores, not any new category of sensitive data — the same chunk content already reaches the patient in the answer itself. No new logging destination or retention policy is introduced. |

</threat_model>

<verification>
- `uv run pytest tests/test_normalize.py -x -q` passes — normalization behavior including non-blocking failure fallback (Task 1).
- `uv run pytest tests/test_hybrid_retriever.py tests/test_rag_engine.py -x -q` passes after the `HybridRetriever` return-shape change (Task 2).
- Real-document normalization check confirms no target Simplified characters survive, and a real query's logs show chunk-level `"Selected chunks:"` detail (Task 3).
</verification>

<success_criteria>
Simplified Chinese source text is normalized to Traditional before it can reach a patient-facing answer (best-effort, non-blocking), and every answer's retrieved context is traceable in logs to specific chunk correlation keys and fused RRF scores — closing the two remaining book-to-skil.md gaps CONTEXT.md scoped into this phase. All nine locked decisions (D-01 through D-09) are now implemented across the four plans in this phase.
</success_criteria>

<output>
Create `.planning/phases/10-rag-retrieval-consolidation-merge-ragengine-sqlite-fts5-and/10-04-SUMMARY.md` when done
</output>
